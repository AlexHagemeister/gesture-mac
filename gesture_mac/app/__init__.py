"""The menu-bar shell. config.py owns the on-disk settings, runtime.py owns
the capture thread that ties tracker, engine, mapper, and performer
together, tracking.py rebuilds the tracker when it fails mid-stream, toast.py is
the pill that names each fired command, menubar.py is the rumps UI,
__main__.py is the entry point."""
